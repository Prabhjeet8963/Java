package static_2;

public class Main {
    public static void main (String args[]){

        long a = Math.round(3.444);
        System.out.println(a);

        // Math = built-in class with math tools
        // Has static methods like round(), sqrt(), pow()
        // Use like Math.round(4.44) — no object needed
        // Java already knows where it is, no import needed
        // Just like a built-in calculator toolbox

    }
}
